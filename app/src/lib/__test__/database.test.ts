// TODO this is all just a placeholder for now
import {jest} from '@jest/globals';
import * as db from "../database";

describe("database tests", () => {
  it("should pass (just a placeholder)", () => {
    expect(true).toBeTruthy();
  });
});
